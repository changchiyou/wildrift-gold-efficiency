---
layout: null
---
<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
    <channel>
        <title>{{ site.title }}</title>
        <description>{{ site.description }}</description>
        <link>{{ site.url }}{{ site.baseurl }}</link>
        <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
        <pubDate>{{ site.time | date_to_rfc822 }}</pubDate>

        {%- assign default_paths = site.pages | map: "path" -%}
        {%- assign page_paths = site.header_pages | default: default_paths -%}
        {%- if page_paths -%}
            {%- for path in page_paths -%}
                {%- assign my_page = site.pages | where: "path", path | first -%}
                {%- if my_page.title and my_page.url != "/" -%}
                    <item>
                    <title>{{ my_page.title }}</title>
                    <description>{{ my_page.description }}</description>
                    <link>{{ site.url }}{{ my_page.url }}</link>
                    <guid isPermaLink="true">{{ site.url }}{{ my_page.url }}</guid>
                    <pubDate>{{ my_page.date | date_to_rfc822 }}</pubDate>
                    </item>
                {%- endif -%}
            {%- endfor -%}
        {%- endif -%}
    </channel>
</rss>